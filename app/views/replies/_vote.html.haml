%br/

=link_to  question_votes_path(@question,@answer,:vote_count => 1 ), :method => :post, :class =>'btn btn-primary' do
  %span.glyphicon.glyphicon-chevron-up


=link_to  question_votes_path(@question,@answer,:vote_count => -1 ), :method => :delete, :class =>'btn btn-danger' do
  %span.glyphicon.glyphicon-chevron-down
