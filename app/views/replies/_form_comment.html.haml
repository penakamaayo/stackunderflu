%p
  = form_for comment, :url => question_reply_replies_path(question,answer) do |f|
    .field
      = f.text_area :body, :required => true

    .field
      / = f.label :user_id
      = f.hidden_field :user_id, :value => "#{current_user.id}"
    .actions
      = f.submit 'Submit Comment', :class => "btn btn-primary"
